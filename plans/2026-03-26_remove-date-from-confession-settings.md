# План: Убрать дату с вкладок Исповедь и Настройки

## Задачи
- [x] Найти элемент даты в `tab-church-today` (Исповедь) и удалить
- [x] Найти элемент даты в `tab-settings` (Настройки) и удалить
- [x] Убрать вызов `updateHeaderDate()` из `switchTab()` для этих вкладок
- [x] Проверить, что дата остаётся только на `tab-church-empty` (Церковный календарь)
- [x] Протестировать переключение между вкладками

## Статус: Завершён

## Выполненные изменения

### index.html
- Удалён Date Header из вкладки «Исповедь» (строки 162-166)
- Удалён Date Header из вкладки «Настройки» (строки 355-359)

### src/main.js
- Удалена функция `updateHeaderDate()`
- Удалены вызовы `updateHeaderDate()` из кнопок навигации по датам

## Результат
Дата отображается только на вкладке «Церковный календарь» (`tab-church-empty`) через элементы `card-date-day` и `card-date-month`.
