# План: Вернуть дату в глобальный заголовок для вкладки Церковный календарь

## Задачи
- [x] Вернуть элемент `<span id="header-date">` в глобальный заголовок
- [x] Создать функцию `updateHeaderDate()` для обновления даты
- [x] Добавить вызов `updateHeaderDate()` при переключении на вкладку «Церковный календарь»
- [x] Добавить вызов `updateHeaderDate()` в кнопки навигации по датам
- [x] Вкладки «Исповедь» и «Настройки» — уже очищены от даты (сделано ранее)

## Статус: Завершён

## Выполненные изменения

### index.html
- Возвращён элемент `<span id="header-date">` в глобальный заголовок (строка 34)

### src/main.js
- Создана функция `updateHeaderDate()` для обновления даты в заголовке
- Добавлен вызов `updateHeaderDate()` при переключении на вкладку `church-empty`
- Добавлен вызов `updateHeaderDate()` в кнопках навигации по датам (prevDateBtn, nextDateBtn)

## Результат
- Дата отображается **только** на вкладке «Церковный календарь» в глобальном заголовке
- Вкладки «Исповедь» и «Настройки» **не имеют** даты
- Дата обновляется при переключении дат (кнопки ← →)
