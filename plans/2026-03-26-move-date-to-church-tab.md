# План: Удалить дату из вкладок Исповедь и Настройки

## Решение
Так как заголовок приложения общий для всех вкладок, нужно:

1. Удалить дату из глобального заголовка (она будет скрыта везде)
2. Добавить заголовок с датой внутрь вкладки «Церковный календарь»
3. Обновлять дату при переключении дат

## Задачи
- [ ] Удалить `<span id="header-date">` из глобального заголовка
- [ ] Добавить заголовок с датой внутрь `tab-church-empty` (Церковный календарь)
- [ ] Обновить функцию `updateHeaderDate()` для работы с новым элементом
- [ ] Вкладки «Исповедь» и «Настройки» останутся без даты

## Статус: Ожидает подтверждения
